# private-aks-bicep-demo

Deploy the cluster with azure cli

`
az deployment group create --resource-group 'aks-playground-eastus-eshop-dev-rg' --template-file main.bicep --parameters parameters.json
`
